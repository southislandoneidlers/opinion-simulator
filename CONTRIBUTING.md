# Contributing (reconstructed stub)

- Read `AGENTS.md` and `docs/README.md` first; append handoffs for every task.
- Tests target public seams; run `npm test` before handing off.
- Never commit credentials; secrets belong in OS keychains only.
- License: Apache-2.0 (finalized at v1.0).
